import {StSize, StStyle} from "../../enums";

export enum StCheckboxSize {
    XS = StSize.XS,
    SM = StSize.SM,
    MD = StSize.MD,
    LG = StSize.LG,
}

export enum StCheckboxStyle {
    SOLID = StStyle.SOLID,
    OUTLINE = StStyle.OUTLINE,
    SUBTLE = StStyle.SUBTLE,
    PLAIN = StStyle.PLAIN,
    INVERTED = StStyle.INVERTED,
}
